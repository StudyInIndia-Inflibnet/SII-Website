<div class="panel panel-default panel-border-color panel-border-color-custom">
    <div class="panel-body" id="menu">
        <br />
        <a href="@Url.Content("~/")admission/student" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Basic" ? "btn-primary" : "btn-default")">Basic information</a>
        <a href="@Url.Content("~/")admission/student" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Academic" ? "btn-primary" : "btn-default")">Academic information</a>
        <a href="@Url.Content("~/")admission/student" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Background" ? "btn-primary" : "btn-default")">Background information</a>

        @*@if (TempData.Peek("Cust_Id").ToString() != "0")
            {
                <a href="~/Customer/Basic?Id=@TempData.Peek("Cust_Id").ToString()" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Basic" ? "btn-primary" : "btn-default")">Basic</a>
            }
            else
            {
            }
            <a href="~/Customer/Passport?Id=@TempData.Peek("Cust_Id").ToString()" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Passport" ? "btn-primary" : "btn-default")">Passport</a>
            <a href="~/Customer/Visa?Id=@TempData.Peek("Cust_Id").ToString()" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Visa" ? "btn-primary" : "btn-default")">Visa</a>
            <a href="~/Customer/Travel?Id=@TempData.Peek("Cust_Id").ToString()" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Travel" ? "btn-primary" : "btn-default")">Travel History</a>
            <a href="~/Customer/Communication?Id=@TempData.Peek("Cust_Id").ToString()" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Communication" ? "btn-primary" : "btn-default")">Communication</a>
            <a href="~/Customer/Bank?Id=@TempData.Peek("Cust_Id").ToString()" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Bank" ? "btn-primary" : "btn-default")">Bank Details</a>
            <a href="~/Customer/Documentation?Id=@TempData.Peek("Cust_Id").ToString()" class="btn btn-lg @(TempData["ActiveMainTab"].ToString()=="Documentation" ? "btn-primary" : "btn-default")">Documentation</a>*@

        <a class="btn btn-lg btn-danger pull-right" href="~/Customer/Basic">Create New</a>
    </div>
</div>
<style>
    .nav.aside-custom-menu a {
        color: #40404c;
        font-size: 14px;
        padding: 10px;
        float: left;
        width: 100%;
    }

        .nav.aside-custom-menu a i {
            color: #616161;
            font-size: 19px;
            margin-right: 3px;
            min-width: 19px;
            text-align: center;
            vertical-align: middle;
        }
</style>
